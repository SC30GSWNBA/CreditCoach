"""Gradio chat UI for CreditCoach.

Start it with:
    uv run python -m creditcoach.app            # local only, at http://127.0.0.1:7860
    uv run python -m creditcoach.app --share    # also create a public link to share with teammates

See ``main.py`` for the options and the login settings.
"""
